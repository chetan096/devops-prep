pipeline {
   agent any
   tools{
      maven 'Maven3'
	    jdk 'Java'
   }
   stages {
     stage('Checkout'){
	     steps {
	      checkout scm
	     } 
     }
	 stage('Build'){
	   steps {
	     echo 'Building the project again'
	   }
	 }
   }
}